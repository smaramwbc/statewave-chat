# Changesets

This directory stores changeset files generated by `pnpm changeset`.

Each file describes a semver bump and a summary of what changed.
Run `pnpm version-packages` to consume them and bump package.json files.
